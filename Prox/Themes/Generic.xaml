<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Prox.Models.CustomeControls.CustomeControls.CCFileBox">


    <Style TargetType="{x:Type local:CCFileBox}">
        <Setter Property="Height" Value="100"/>
        <Setter Property="Width" Value="auto"/>
        <Setter Property="Background" Value="#AD666666"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CCFileBox}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="11">
                        <Grid>
                            <Label Content="{TemplateBinding FileName}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Foreground="#FFF" Margin="30 0 30 5" FontFamily="Segoe UI Semibold" FontSize="14"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
